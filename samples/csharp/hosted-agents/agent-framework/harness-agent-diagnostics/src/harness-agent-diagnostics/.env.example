# Foundry project endpoint is injected in hosted containers.
# Use DefaultAzureCredential for authentication; never store keys, tokens, tenant IDs, subscriptions, or connection strings here.
# FOUNDRY_PROJECT_ENDPOINT=https://<account>.services.ai.azure.com/api/projects/<project>

AZURE_AI_MODEL_DEPLOYMENT_NAME=
